{
  "name": "@peerkit-video-chat/desktop",
  "version": "0.0.0",
  "private": true,
  "description": "Electron desktop app for the peerkit-video-chat showcase",
  "type": "module",
  "main": "./out/main/index.js",
  "scripts": {
    "build": "tsc --build && electron-vite build",
    "dev": "electron-vite dev",
    "start": "electron-vite preview"
  },
  "dependencies": {
    "@peerkit-video-chat/core": "*",
    "@peerkit/api": "^0.1.0-alpha.9",
    "@peerkit/peerkit": "^0.1.0-alpha.9"
  },
  "devDependencies": {
    "electron": "^32.0.0",
    "electron-vite": "^5.0.0",
    "vite": "^7.3.3"
  }
}
