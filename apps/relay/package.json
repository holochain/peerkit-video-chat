{
  "name": "@peerkit-video-chat/relay",
  "version": "0.0.0",
  "private": true,
  "description": "PeerKit relay server — rendezvous point for peer discovery",
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@peerkit/api": "^0.1.0-alpha.10",
    "@peerkit/peerkit": "^0.1.0-alpha.10",
    "@peerkit/transport-libp2p-nodejs": "^0.1.0-alpha.10"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
